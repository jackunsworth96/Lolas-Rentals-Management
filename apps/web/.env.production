# Production (Vercel): same-origin /api is proxied to Render via vercel.json rewrites.
VITE_API_URL=/api

# Remove or set to false once the site is stable to disable the WhatsApp error banner.
VITE_BETA_ERROR_NOTICE=true
